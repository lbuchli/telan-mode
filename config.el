;;; config.el --- description -*- lexical-binding: t; -*-

;;; Code:
(def-package! telan-mode
  :commands (telan-mode)
  ;; :config
  ;; (setq )
)

(load! "telan-mode")

(provide 'config)
;;; config.el ends here
